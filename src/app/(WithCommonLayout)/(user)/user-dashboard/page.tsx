const UserDashboard = async () => {
  // const { data: myProduct } = await getAllMyProducts([
  //   { name: "sort", value: "-upvote" },
  //   { name: "limit", value: 10 },
  // ]);

  // const { data: allMyTag } = await getAllMyTag();

  return (
    <div>
      {/* <UserDashboardCard product={myProduct} tags={allMyTag} /> */}
      <p>User dashboard</p>
    </div>
  );
};

export default UserDashboard;
